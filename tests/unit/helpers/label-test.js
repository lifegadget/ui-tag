import { label } from 'dummy/helpers/label';
import { module, test } from 'qunit';

module('Unit | Helper | label');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = label([42]);
  assert.ok(result);
});
