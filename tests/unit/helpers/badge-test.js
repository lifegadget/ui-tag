
import { badge } from 'dummy/helpers/badge';
import { module, test } from 'qunit';

module('Unit | Helper | badge');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = badge([42]);
  assert.ok(result);
});

