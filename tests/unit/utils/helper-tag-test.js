import helperTag from 'dummy/utils/helper-tag';
import { module, test } from 'qunit';

module('Unit | Utility | helper tag');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = helperTag();
  assert.ok(result);
});
