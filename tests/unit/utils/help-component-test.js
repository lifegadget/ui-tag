import helpComponent from 'dummy/utils/help-component';
import { module, test } from 'qunit';

module('Unit | Utility | help component');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = helpComponent();
  assert.ok(result);
});
